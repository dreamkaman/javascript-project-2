{{#each this}}
<div class="card-photo" ">
<div data-card="{{id}}" class="card-no-image" >

{{#if poster_path}}
<img data-card="{{id}}" class="card-img" src="{{poster_path}}" alt="{{title}}" loading="lazy" />
{{/if}}

</div>

<p class="id-card is-hidden">{{id}}</p>

    <div class="card-info">
        <p class="card-name">
            {{title}}
        </p>
        <p class="card-genre-date">
            {{genre_ids}} | {{release_date}}
        </p>
        
    </div>
</div>
{{/each}}